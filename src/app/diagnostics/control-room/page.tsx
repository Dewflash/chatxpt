import { ControlRoom } from "@/components/control-room";

export const dynamic = "force-dynamic";

/** Retained legacy prototype for authorised recovery and comparison only. */
export default function LegacyControlRoomDiagnosticPage() {
  return <ControlRoom />;
}
