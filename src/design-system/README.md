# Design-system public entrypoint

Role 4 owns the shared visual system and will publish accepted tokens and base components from `index.ts` for Role 5 to consume.

The entrypoint is intentionally empty until Role 4 completes its plan review. This avoids Role 1 silently choosing another owner's visual contract while still reserving the collision-free public module path.
