# Persist streamer profiles across streams with local recovery

- **Type:** Added
- **Role:** Role 1
- **Issue/PR:** pending
- **Summary:** New Twitch sessions hydrate the complete saved streamer profile before bootstrap, current-stream game metadata stays separate from saved defaults across extraction/AI/quest/UI consumers, realtime refreshes preserve same-account storage health, and Studio exposes an established device-local preset fallback with explicit conflict recovery. Verified Twitch category changes and Generic capture update the current stream without mutating saved defaults; calibrated capture can explicitly update both.
- **Integration impact:** Adds the public profile repository, account/profile connection view state, optional current-session game context, profile-specific optimistic concurrency, current-game commands routed through the quest engine, two forward-only Supabase ownership migrations, and Role 4 profile/health UI consumption. A game switch clears incompatible gameplay/director context and cancels an in-flight quest. Viewer voting including `1`/`2`/`3` chat fallback and OBS continue to consume the same projected cycle revision.
- **Verification:** The final current-game/profile authority regression run passed 7 files and 109 tests. `npm run smoke` passed all eight canonical memory-backed Studio/gameplay/EventSub/hosted-vote/OBS/page checks with process-only dummy Twitch configuration. `npm run check` passed 109 test files and 877 tests, production build, role boundaries, hygiene, evidence checks, and client-secret scans. Browser verification rendered the local fallback with separate account/input/storage health, no console warnings, and no horizontal overflow; an earlier edit/reload check preserved the local default game at 1280px desktop and 390px mobile.
- **Reality status:** Memory/runtime behavior and browser-storage logic are automated-test and local-browser proven; SQL is statically covered and a pgTAP identity-isolation suite is committed but was not executable in this clone. There is no linked Supabase/Vercel CLI state or credentials, so Cloud, Preview, server-restart, second-browser, and owner-visible evidence remain unproven.
