# Recover the real Twitch, gameplay, and voting rehearsal

- **Type:** Fixed
- **Role:** Role 1
- **Issue/PR:** pending
- **Summary:** Gameplay Capture now explains camera failures and recovers resources; stale Studio commands rebase safely while capture is advancing authority; development HMR no longer hides typed Studio/overlay errors; vote-close revalidation accepts current-envelope gameplay and audience evidence without weakening explicit cycle checks; and Next.js development indicators stay out of the OBS Browser Source.
- **Integration impact:** Additive OBS browser-adapter failure reasons affect Role 1 and Role 2 consumers. The Studio runtime keeps broadcaster identity checks while retrying current revisions. Role 3 close-time validation rebases only revision metadata and fills null session-scoped cycle IDs; wrong session, evidence class, or explicit non-null cycle still fails closed.
- **Verification:** Focused OBS, Studio, API response, quest-engine, scheduler, orchestrator, encrypted-store, and deployment-config tests pass; a real local Twitch stream delivered online/offline events and privacy-safe chat aggregates; authenticated OBS Virtual Camera Minecraft snapshots produced exactly three deterministic fallback quests; one hosted-board vote was accepted. After rebasing the persistent-overlay/OAuth/capture integration from `e8c5266`, `npm.cmd run check` passes with 107 test files / 843 tests, the production build, and the built-client secret scan. A locally authenticated OBS WebSocket inspection confirmed an enabled 1920×1080 `browser_source` using the signed read-only overlay route, and an OBS source screenshot confirmed its clean transparent idle render without the development badge.
- **Reality status:** The Twitch stream, EventSub activity, chat aggregate, OBS Virtual Camera gameplay, authoritative Studio session, hosted-board vote, installed/active Twitch Local Test panel, and actual OBS Browser Source render were real and memory-backed locally. A separate explicitly diagnostic run used two locally signed fake viewer identities to prove vote privacy, duplicate protection, automatic winner activation, progress, success, rewards, and a completed canonical OBS render; it also correctly rejected a winning fixture that depended on unavailable hotbar/inventory evidence. The post-fix real Twitch rehearsal closed with zero votes, so a real Twitch-issued winner remains unproven. Supabase Cloud and Vercel were deliberately left out of the continued test run.
