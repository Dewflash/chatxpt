## Role 1

- Added browser-safe streamer setup and session service command schemas for Role 4 without routing them through Role 3's quest engine authority.
- Extended the diagnostic UI gateway and `FetchUiGatewayClient` to acknowledge setup/session commands with typed current-revision results and readiness fixtures while keeping real Twitch/OBS/session operations unclaimed.
