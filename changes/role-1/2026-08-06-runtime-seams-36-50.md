# Implement runtime seams for quest timers, intervention, emergency latch, and progress

- **Type:** Added
- **Role:** Role 1
- **Issue/PR:** #36, #37, #38, #48 recovery, #50 / pending
- **Summary:** Added canonical timer/progress/emergency-clear commands, a durable `emergencyPaused` authoritative latch, the Role 1 intervention coordinator, and the recovered vote-close scheduler spine.
- **Integration impact:** Role 1 now blocks new intelligence-ready proposals while emergency pause is latched, clears the latch through an authenticated state command, supplies neutral progress validation context to Role 3, composes intervention policy before Role 2 candidate generation, and can sweep due voting cycles into deterministic `system.vote-close` commands. Automatic signal progress no longer requires audience data when the cited gameplay evidence is valid; missing gameplay, unknown, unsupported, disallowed, weak, stale, or regressive evidence still fails closed.
- **Verification:** `npm run test -- src/quest-engine/engine.test.ts src/quest-engine/outcomes.test.ts`; `npm run check`.
- **Reality status:** Scheduler, memory/Supabase adapters, orchestrator behavior, and contracts are covered by automated tests. Live worker/Cron execution and Role 3's unresolved vote-close winner policy still depend on the remaining integration/review path.
