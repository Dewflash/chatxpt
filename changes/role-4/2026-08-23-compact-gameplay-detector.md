# Compact Gameplay Engine detector

- Aligned detector values to the right of their matching reading labels.
- Replaced pre-capture `Waiting` values with `—` and made the detector heading report whether the gameplay feed needs connection, is connecting, or is connected.
- Removed planned `Coming soon` reads from the live detector surface.
- Reduced the Gameplay Engine-only spacing around the section navigation without changing other Studio pages.
