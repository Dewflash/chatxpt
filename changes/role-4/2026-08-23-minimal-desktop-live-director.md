# Make Desktop Live Director a compact interactive status HUD

- **Type:** Changed
- **Role:** Role 4
- **Issue/PR:** pending
- **Superseded scope:** D-096 and `2026-08-23-live-director-active-controls.md` replace only the disabled `Studio only` cancellation boundary with confirmed canonical cancellation and immediate completion; the compact presentation in this fragment remains current.
- **Summary:** The private persistent companion keeps the compact four-reading HUD and adds live duration, truthful chat activity, and the current gameplay state. While exactly three recommendations await approval, it shows the mode-appropriate routing control. While a quest is active, it shows the selected quest, authoritative time remaining, and a disabled `Cancel quest` affordance labelled `Studio only`.
- **Integration impact:** The surface consumes the broadcaster-authorised `StreamerViewModel` and sends only the same canonical generation or recommendation-approval commands as Studio through its permanent private grant. Role 1 re-resolves and validates the current broadcaster session and Role 3 remains authoritative; the active countdown derives from the canonical deadline, and the companion never emits a cancellation command, casts a viewer vote, selects a winner, or owns session, analysis, audience, persistence, or quest logic. Desktop defaults remain 360×240, legacy defaults migrate automatically, and custom saved sizes remain unchanged.
- **Verification:** The active-quest focused component/client suite passes 2 files / 11 tests. The complete `npm run check` gate passes 122 files / 1,018 tests, lint, TypeScript, boundaries, hygiene/evidence/runbook checks, production build, and client-secret scans. The earlier rebuilt macOS package passes Electron smoke for the preload bridge, window creation, always-on-top, click-through, hide/show, and secure storage.
- **Reality status:** Memory-backed integration evidence proves the Live Director command and Studio converge on one authoritative voting revision, retries are idempotent, and public OBS grants cannot use the command. A real Twitch-linked owner click, viewer vote, Minecraft-focus check, and live OBS/Extension observation still require the owner runtime test.
