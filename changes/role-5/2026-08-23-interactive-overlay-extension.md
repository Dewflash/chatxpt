# Stabilise the shared broadcast quest frame

- **Type:** Changed
- **Role:** Role 5
- **Issue/PR:** pending
- **Summary:** The Twitch Extension now uses the same compact quest-frame hierarchy as the OBS broadcast overlay while retaining full quest instructions, difficulty, duration, reward, safe selection, vote confirmation, acknowledgement, recovery, reactions, and private session points. The standalone Twitch Asset Hosting package follows the same presentation and no longer shows backend configuration copy to viewers. The OBS frame drops its redundant `What this means` box and keeps time-left and connection status fixed in the top-right header column as sidequest phases and titles change.
- **Integration impact:** Updates the public `@/viewer` presentation and the Role 1-owned seven-file Twitch upload artifact without changing Core view models, vote authority, authentication, persistence, realtime, or OBS read-only permissions. The hosted Quest Board remains unchanged.
- **Verification:** The original focused viewer/Twitch-asset pass remained green at 2 files / 34 tests, and its full `npm run check` passed 122 files / 999 tests plus production build. For the OBS refinement, the focused viewer surface suite passed 1 file / 29 tests; a local 1280×720 fixture-browser check confirmed a non-wrapping top-right status column, matching header/status top edges, no `What this means` copy, and no overlay buttons. One `npm run check` attempt passed lint, TypeScript, boundaries, hygiene, evidence, and runbook gates before stopping on concurrent non-viewer tests after 999 passes. A later shared-tree rerun stopped at an unrelated `streamer-authorized-client.tsx` type error after concurrent profile-contract work added required `desktopDirector` defaults. A standalone production build passed before that later edit; the focused documentation gates and `git diff --check` pass.
- **Reality status:** Canonical fixture data and the dependency-free upload package were exercised locally. No real Twitch-issued JWT, Local/Hosted Test vote, external viewer, Supabase Cloud, or OBS Browser Source run is claimed by this visual pass.
