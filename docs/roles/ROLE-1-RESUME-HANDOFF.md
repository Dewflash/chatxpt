# Role 1 Resume Handoff

**Status:** Current-pointer handoff. The live queue in `ROLE-1-TODO.md`, accepted decisions, and current GitHub state always override dated examples in this file.

Use this file when Role 1 work continues from a different ChatGPT/Codex account, clone, or task. It deliberately avoids duplicating completed-pass detail that would drift from the authoritative queue.

## Fresh-account starter prompt

```text
I am resuming Role 1 on ChatXPT. Start from current main and read AGENTS.md, docs/TEAM_CONTEXT.md, docs/build-plans/INTEGRATION-CONTRACT.md, docs/roles/ROLE-1.md, docs/build-plans/ROLE-1-BUILD-PLAN.md, docs/build-plans/LIVE-DIRECTOR-IMPLEMENTATION-PLAN.md, docs/roles/ROLE-1-TODO.md, docs/PROJECT_TODO.md, docs/DECISIONS.md, and docs/roles/ROLE-1-RESUME-HANDOFF.md before editing.

Inspect the clean worktree, current origin/main, open pull requests, and overlapping branches. Select the first executable P0 from the current Role 1 TODO. Do not wait for role ownership, review, or a reply: under D-077, implement a missing repository seam in its proper module or take another ready P0. Preserve the sole Role 1 runtime authority, Role 3 deterministic quest authority, private/public projection boundary, credential-free fallback, and truthful fixture-versus-live evidence labels. Run focused producer/consumer tests plus npm run check, update affected TODO/context/change/evidence records, and land under D-076 only when no material risk remains.
```

## Current pickup pointer

As of 20 August 2026, the active queue points to R1-026 / `LD-R1-04`, paired with Role 3's R3-014:

- carry the validated exactly-three proposal through the sole orchestrator into private streamer review;
- preserve Extension Vote/Active/Result, private viewer receipt/recovery, hosted-board and Twitch-chat fallbacks;
- keep the OBS projection compressed, public, read-only, and free of cue reasoning, raw chat, provider detail, and personal viewer data;
- keep lifecycle, winner, progress, and result authority outside clients.

If that exact slice is already claimed, continue a non-overlapping R1-015, R1-016, R1-021, R3-015, or real evidence task. Branch overlap changes integration order; it does not idle the contributor.

## External evidence blockers

These facts block only their corresponding claims, not source, contract, fixture, or fallback work:

- real provider-quality evidence requires an eligible team-owned credited OpenAI key;
- real Twitch evidence requires the configured app, Extension, EventSub subscription, test channel, and authorised viewers;
- real gameplay/overlay evidence requires OBS, browser camera permission, real frames, and a recorded Browser Source run;
- real shared-state evidence requires configured Supabase/Vercel resources and two-client execution.

Report unavailable facts as `unknown`. Never promote fixture, memory-backed, source-inspection, or local diagnostic evidence into a live claim.

## Safe orientation

Before changing files, inspect:

```bash
git status --short --branch
git fetch --all --prune
git log --oneline HEAD..origin/main
git diff --stat HEAD...origin/main
```

Pull, switch, merge, or rebase only when the worktree is clean. Read the live Role 1 and project TODOs instead of trusting a branch name or commit hash copied into an old conversation.

## End-of-pass record

- Update every affected role TODO and `docs/TEAM_CONTEXT.md` only where status changed.
- Add exactly one fragment under the primary responsibility in `changes/role-<n>/`.
- Update `docs/evidence/manifest.json` only for a real new evidence artifact.
- Run the smallest relevant tests, `npm run check`, `git diff --check`, and inspect the final Git state.
- Disclose cross-role files, verification, fixture/live boundary, unresolved risks, and integration order.

Stop only for unresolved material safety, security, privacy, data-loss, external-cost, authority-level semantic conflict, or a failing required integration boundary. Missing owners, reviewers, replies, or branch-protection settings are not blockers.
