# Decision Log

`Accepted` entries are authoritative. `Proposed` entries remain open defaults for team discussion.

| ID | Status | Proposal or decision | Reason | Date | Participants |
| --- | --- | --- | --- | --- | --- |
| D-001 | Accepted | Keep one reusable Next.js/TypeScript ChatXPT system with separate streamer, embedded Twitch, viewer, fallback, overlay, and server routes around a platform-neutral core. Twitch is the current adapter rather than the product architecture. | Produces one deployable MVP while keeping platform capabilities and future adapters replaceable. | 2026-08-03 | Project owner |
| D-002 | Superseded by D-010 | Support one battle-royale-style golden scenario first. | The product and contracts must not be narrowed to battle royale; a rehearsed demonstration scenario may still be used for reliability. | 2026-08-03 | Project owner |
| D-003 | Accepted | Keep deterministic generation as a permanent fallback. | Protects the demo from credentials, latency, quotas, and outages. | 2026-08-03 | Project owner, team confirmation via Codex task |
| D-004 | Superseded by D-013 | Use local browser transport before a database/WebSocket service. | The accepted multi-device MVP needs shared persistence and realtime state; local transport remains only a credential-free fallback. | 2026-08-03 | Project owner |
| D-005 | Superseded by D-009 | Require producer approval before activating a quest. | The project owner delegated approval, veto, and activation mechanics to Role 3 instead of prescribing a mandatory global default. | 2026-08-03 | Project owner |
| D-006 | Accepted | Twitch is the only supported platform for the current build. Other streaming platforms may appear only as disabled `Coming Soon` options; no adapters or platform-specific features are implemented for them. | Concentrates the five roles on one complete, credible Twitch workflow while preserving the longer-term cross-platform direction. | 2026-08-03 | Project owner |
| D-007 | Accepted | Use a hybrid streamer experience: ChatXPT Studio is the primary full setup and management app; an embedded Twitch Live Config page provides compact controls during a stream; the Twitch Extension serves viewers; OBS displays automatic broadcast visuals. | Preserves a complete ChatXPT product and persistent configuration while keeping routine live controls inside Twitch and avoiding duplicated full interfaces. | 2026-08-03 | Project owner |
| D-008 | Accepted | The MVP uses one private, platform-neutral participation service. Twitch Extension voting is primary; a hosted ChatXPT Viewer Quest Board is the first fallback; `1`/`2`/`3` Twitch-chat voting is the final fallback. The internal contract may support future integrations, but a public API, SDK, and non-Twitch adapters are out of scope. | Preserves participation when a platform capability or Extension is unavailable, keeps authoritative voting logic outside UI clients, and prepares future integrations without expanding the Twitch-only MVP. | 2026-08-03 | Project owner |
| D-009 | Accepted | Role 3 independently decides quest intervention, approval, veto, automatic/manual activation, interruption, and emergency-control mechanics, informed by Role 2's behavioural intelligence and saved streamer preferences. | These are component-level quest-engine decisions; the project owner should only be involved when they cross product scope, role ownership, shared contracts, or non-negotiable safety requirements. | 2026-08-03 | Project owner |
| D-010 | Accepted | ChatXPT targets game streamers across audience sizes, play styles, and game genres. Shared contracts and engine behaviour remain game-neutral; the team may use one reliable real game or prepared real-gameplay scenario in the demo without making it a product restriction. | Supports cases from battle royale to games such as Brawl Stars while keeping the five-day prototype demonstrable. | 2026-08-03 | Project owner |
| D-011 | Superseded by D-028 | Simulated gameplay state is acceptable in the MVP when clearly disclosed and implemented behind a replaceable extraction interface; the main workflow must still demonstrate real AI contribution and functioning end-to-end behaviour. | The project owner subsequently required real captured gameplay and real Twitch activity in the judged workflow. | 2026-08-03 | Project owner |
| D-012 | Accepted | Retain only data necessary for the experience: do not permanently retain raw chat, prefer aggregate session metrics and quest outcomes, support anonymous fallback participation, and keep rewards non-monetary and non-wagering. | Meets prototype privacy and safety needs without creating unnecessary personal-data or financial risk. | 2026-08-03 | Project owner |
| D-013 | Accepted | Use Supabase Free for MVP persistence and realtime state and Vercel for the reusable Next.js deployment. Role 1 owns both service accounts, production secrets, schema/realtime boundaries, and production deployment. Preserve local mock transport as a fallback. | Connects Studio, Twitch surfaces, fallback viewers, and OBS across devices within the five-day build while keeping infrastructure small and replaceable. | 2026-08-03 | Project owner |
| D-014 | Superseded in part by D-072 | Roles 2 and 3 both have AI decision authority with explicit boundaries. Role 2 owns provider adapters, context, signal-analysis AI, and reliability evaluation; Role 3 owns quest-domain AI objectives, generation instructions, quality, and engine use. Provider/model adoption requires their joint recommendation to Role 1. | D-072 directly approves the judged-MVP provider/model and removes the recommendation as an adoption gate; the Role 2/3 evaluation and deterministic-authority boundaries remain. | 2026-08-03 | Project owner |
| D-015 | Superseded by D-071 | Role 1 may inspect, redirect, assist, and modify any role for integration, safety, deadline recovery, or an owner-requested fix. Notify the affected owner and request review whenever practical; urgent demo fixes must be minimal, immediately disclosed, and recorded. | D-071 opens cross-role contribution to every contributor and makes Role 1 deconfliction an ordinary duty rather than a special override. | 2026-08-03 | Project owner |
| D-016 | Superseded in part by D-071 | For the current MVP planning pass, Role 2 has scoped authority to decide the build plans for Roles 4 and 5: outcomes, surface/flow coverage, priorities, required states, AI/data requirements, mock/live boundaries, milestones, acceptance criteria, exclusions, and handoff order. Roles 4 and 5 review for feasibility and implement their respective plans, retaining detailed visual, interaction, accessibility, component, and code decisions that do not contradict the plan. Role 2 may revise the plans but may not edit or implement Role 4/5 source; Role 1 remains final authority. | Role 2 remains the plan responsibility lead; D-071 removes the source-edit restriction and all role-based permission gates. | 2026-08-03 | Project owner |
| D-017 | Accepted | Role 1 defines the required phases, order, outcomes, deadlines, acceptance criteria, and integration boundaries for the Role 2 and Role 3 build plans. Joelyrk and L0pch retain the component decisions explicitly assigned to them within each phase and proceed autonomously after answering that phase's decision batch, escalating only cross-role, shared-contract, cost, safety, or scope issues. | Keeps the five-day effort coordinated without turning Role 1 into the designer of Role 2's intelligence or Role 3's engine mechanics. | 2026-08-03 | Project owner |
| D-018 | Accepted | Roles 1-3 work concurrently. Role 1 publishes a thin version-one contract skeleton during its first pass; Roles 2 and 3 begin immediately with role-owned fixtures and adapters and propose shared-contract changes without waiting for the full platform. | Removes sequential blocking while keeping integration through one reviewed contract boundary. | 2026-08-03 | Project owner |
| D-019 | Accepted | Role 2 delivers the separate Role 4 and Role 5 build plans in its first working pass, collects one feasibility response from each owner, revises once, and then moves primarily to extraction and AI intelligence. | Unblocks both UI owners early without consuming Role 2's core implementation time or removing their detailed UX authority. | 2026-08-03 | Project owner |
| D-020 | Accepted | The MVP Twitch proof uses a real Twitch application/Extension in Local or Hosted Test, an allowlisted broadcaster and viewer, real chat where possible, and a real OBS browser source. Public Extension approval is not required for the build challenge proof. | Produces authentic Twitch evidence within the deadline without depending on marketplace review timing. | 2026-08-03 | Project owner |
| D-021 | Superseded in part by D-072 | AI operation must remain free for the MVP. Role 2 provides deterministic or algorithmic intelligence when a free model is unavailable or unsuitable, and Role 3 preserves deterministic quest fallbacks; no paid model usage is authorised. | D-072 permits the approved server-side model only against existing team-owned prepaid or promotional credit; it still forbids requiring a contributor to buy quota and keeps the credential-free path mandatory. | 2026-08-03 | Project owner |
| D-022 | Accepted | Streamers customise AI behaviour through understandable experience settings such as personality, tone, creativity, intensity, and safety. Provider and raw model names are not exposed as the normal streamer control. | Gives streamers useful control while keeping provider infrastructure replaceable and the setup understandable. | 2026-08-03 | Project owner |
| D-023 | Accepted | Viewer identity uses Twitch identity when available and supports anonymous fallback. MVP viewer points and community hype are session-scoped; there is no persistent cross-stream reward economy. | Preserves low-friction participation while avoiding unnecessary account, privacy, economy, and moderation scope. | 2026-08-03 | Project owner |
| D-024 | Accepted | Raw Twitch chat may be retained temporarily for debugging and evaluation for no more than 24 hours. It is not permanent product history; aggregate signals and quest/session outcomes remain the preferred retained records. | Supports short-lived debugging evidence while minimising personal-data retention. | 2026-08-03 | Project owner |
| D-025 | Accepted | Role 1 creates Supabase and Vercel preview infrastructure during the first integration phase so the team can integrate continuously against a deployed environment. | Finds multi-device, environment, and deployment problems before the final integration day. | 2026-08-03 | Project owner |
| D-026 | Accepted | Normal ChatXPT operation applies to ChatXPT-enabled streams: the broadcaster installs and connects ChatXPT and grants the required Twitch and gameplay-capture access. Viewers then receive automatic analysis and participation on that broadcaster's stream. Arbitrary channels without broadcaster installation or authorisation are not a normal supported product flow. | Makes capture consent, credentials, configuration, and service expectations explicit while still supporting streamers broadly. | 2026-08-03 | Project owner |
| D-027 | Accepted; streamer access added by D-084 | The visual-analysis Test Lab uses gameplay video or live gameplay owned by the team or explicitly authorised for processing. A controlled hosted fixture proves repeatable capture and analysis; the same owned gameplay streamed through a team-controlled Twitch test channel proves the Twitch capture-to-analysis path. Arbitrary third-party streams are excluded from testing. D-084 makes the Test Lab accessible to streamers without changing these authorised-input and evidence boundaries. | Provides reproducible real-video evidence without processing unapproved streams or presenting fixture output as live evidence. | 2026-08-03 | Project owner |
| D-028 | Accepted | The judged workflow and product demonstration must use real captured gameplay and real Twitch activity. Simulated fixtures are permitted only for automated tests, developer diagnostics, and offline reproducibility; they may not be presented as evidence that live extraction works. If a real signal cannot be determined, ChatXPT reports it as unknown instead of fabricating gameplay state. | Protects the central product claim and makes uncertainty more credible than staged gameplay events. | 2026-08-03 | Project owner |
| D-029 | Accepted | The MVP streamer capture path uses OBS Virtual Camera configured to expose the raw game source to ChatXPT Studio. Role 2 analyses those real frames with a hybrid of lightweight visual algorithms, selective OCR, and optional free vision AI. The controlled Twitch-tab Test Lab proves analysis of Twitch-delivered video; OBS WebSocket or a local companion remains a later upgrade. | Uses the most achievable real capture path for the deadline while keeping extraction replaceable and proving Twitch-delivered-video analysis separately. | 2026-08-03 | Project owner |
| D-030 | Accepted | Role 1 owns the sole ChatXPT application orchestrator. It composes Role 1 inputs with Role 2 public intelligence/candidate ports and Role 3's pure engine port, then authenticates, deduplicates, persists authoritative revisions, and broadcasts role-specific view state. Roles 2/3 do not persist or broadcast lifecycle state; Roles 4/5 render view models and emit commands without business-rule authority. | Prevents duplicate sources of truth and gives the five independently owned modules one explicit runtime composition boundary. | 2026-08-03 | Project owner |
| D-031 | Accepted | Every cross-role seam uses versioned public ports, canonical examples, and producer/consumer contract tests. Each integration wave merges the smallest passing vertical slice into `main`; five components completed and combined only at the end do not meet the integration standard. | Finds incompatible assumptions while they are cheap to correct and makes “works together” an evidenced exit condition. | 2026-08-03 | Project owner |
| D-032 | Superseded in part by D-071 | Role 1 owns collision-prone shared composition files: thin `src/app/` entry points, dependency/lock/config/env files, Supabase migrations/RLS, and integration-test configuration. Roles 2-5 export public modules from owned directories and request shared dependencies through Role 1 unless a scoped exception is granted. | Role 1 still maintains and deconflicts these files; D-071 allows any contributor to edit them after checking overlap and coordinating before merge. | 2026-08-03 | Project owner |
| D-033 | Accepted | Game support is tiered: universal visual signals cover broad motion/activity/downtime/transitions; calibrated adapters may emit specific HUD facts for configured games; official native telemetry is future work. Capabilities and `unknown` are explicit, and unsupported facts are never inferred. | Keeps the reusable all-action-game product claim honest without requiring impossible universal HUD recognition in five days. | 2026-08-03 | Project owner |
| D-034 | Accepted | Role 2's Role 4 and Role 5 plans are separate but synchronised. They must share integration deadlines/dependencies and define public entries, Role 1 route mounts, view models, commands, typed errors, canonical fixtures, complete UI state coverage, contract/evidence requirements, and an early Role 4 design-system handoff that does not block Role 5. | Ensures both UI owners build concurrently against the same runtime rather than creating visually complete but incompatible sites. | 2026-08-03 | Project owner |
| D-035 | Accepted | Authoritative realtime changes use server timestamps, command IDs, expected/current revisions, idempotency, typed errors, atomic persistence before broadcast, and reconnect snapshots. Broadcaster/moderator, viewer, system, and read-only overlay permissions remain distinct. | Prevents duplicate activation/votes, stale overwrites, client-clock authority, and inconsistent screens during reconnects or concurrent commands. | 2026-08-03 | Project owner |
| D-036 | Accepted | Twitch registration/test-mode, OBS Virtual Camera browser capture, Supabase realtime/RLS, Vercel secret separation, and the free-provider/no-credential path receive executed feasibility spikes during the first integration wave even when their complete implementations belong to later phases. | Exposes external and platform blockers early enough for a five-day recovery rather than after component completion. | 2026-08-03 | Project owner |
| D-037 | Accepted | Use a hybrid Supabase data model: relational identifiers, revisions, lifecycle fields, uniqueness, and foreign-key constraints surround versioned JSONB canonical state, event, receipt, and role-view payloads. | Preserves database-enforced concurrency and lifecycle invariants without duplicating or prematurely flattening role-owned quest and intelligence schemas. | 2026-08-03 | Project owner |
| D-038 | Accepted | ChatXPT servers are the only database writers. Clients submit commands through Role 1 services and receive role-sanitised snapshots/realtime notifications; no browser, Twitch Extension, or OBS client may directly mutate authoritative Supabase tables. | Keeps command authentication, permissions, idempotency, revision checks, and sanitisation at the sole application authority while still supporting multi-device clients. | 2026-08-03 | Project owner |
| D-039 | Accepted | Development must work without credentials through an in-memory/local fallback. The team uses one Supabase Free project as the shared preview/demo environment; an isolated production project and stricter environment promotion are deferred until deployment requires them. | Lets all five contributors work immediately while providing one real shared backend for the MVP without unnecessary infrastructure overhead. | 2026-08-03 | Project owner |
| D-040 | Accepted | Permit one active ChatXPT session per broadcaster and use an eight-character fallback room code. An inactive preparing session expires after two hours. A live session continues while broadcaster heartbeats arrive, receives a ten-minute reconnect grace after loss, has no fixed maximum duration, and ends manually, on confirmed Twitch offline status, or when reconnect grace expires. Expiry controls access/lifecycle and does not delete history. | Matches real stream duration, prevents abandoned rooms and duplicate broadcaster state, and gives temporary disconnections a predictable recovery window. | 2026-08-03 | Project owner |
| D-041 | Accepted | Broadcasters have full session authority; moderators receive stream-time quest controls only; authenticated or anonymous viewers may read, vote, and react; system intelligence may submit candidate-ready commands only; OBS receives a sanitised read-only view. Tokens that expire lose their capability and must reauthenticate or use an allowed fallback. Raw chat remains in memory unless Role 2 later requests approved short-lived retention. | Enforces least privilege across every client class and minimises personal-data retention without pre-deciding Role 3 quest mechanics. | 2026-08-03 | Project owner |
| D-042 | Accepted | Roles 4 and 5 use a guided novice workflow: their owner may ask only what to do; Codex selects the first ready phase, explains one bounded pass, and actively coaches them through a small tailored set of relevant design/UX choices with plain-language trade-offs and recommendations. Plan questions are starter examples, not fixed or exhaustive scripts. Codex handles routine technical/Git choices, records material answers, and requests one final approval before commit/push/PR. | Keeps inexperienced collaborators productive and authoritative over their UX, prompts real design thinking for each surface, and avoids requiring repository coordination or implementation terminology first. | 2026-08-03 | Project owner |
| D-043 | Superseded in part by D-071 | Any unresolved UI dependency, contract ambiguity, or integration requirement is converted into explicit Role 1 or target-owner work with an owner, required-by phase, issue, and accepted fixture/disabled interim path. Roles 4/5 are not asked to solve another role's implementation and may not invent it inside UI code. | UI modules still may not invent backend authority, but D-071 allows any contributor to implement the missing capability in its proper module without waiting for the target owner. | 2026-08-03 | Project owner |
| D-044 | Accepted | Add a platform-neutral `system.quest-tick` command. Role 1 authenticates, schedules, deduplicates, and supplies authoritative time; the command never names a target lifecycle state. Role 3 alone decides expiry, cooldown entry, and return to idle from absolute deadlines. | Preserves Role 3 lifecycle authority while giving the pure engine a deterministic, replayable source of time progression without ambient timers or an overloaded intelligence command. See issue #36. | 2026-08-04 | Project owner; Role 3 |
| D-045 | Accepted | Role 1 invokes Role 3's public intervention policy before Role 2 candidate generation. The canonical order is intelligence, intervention, candidate generation, validation/assembly/lifecycle, then Role 1 persistence and broadcast. Role 1 supplies saved profile, session/emergency state, and a platform-neutral recent-quest summary; Role 2 is not called when intervention is denied. | Avoids provider work during unsuitable moments, keeps intervention rules with Role 3, and keeps authoritative composition, history retrieval, and side effects with Role 1. See issue #37. | 2026-08-04 | Project owner; Role 3 |
| D-046 | Accepted | Persist an application/session-level `emergencyPaused` latch under Role 1. An authorised emergency-pause command sets the latch and reaches Role 3 to cancel the current cycle; the latch blocks new intervention until an explicit authenticated clear-emergency command succeeds. Ordinary resumable pause remains unavailable until a complete paused-state/resume contract is accepted. | Makes emergency stop durable across clients and reconnects without giving the UI or pure engine persistence authority or silently inventing ordinary pause semantics. See issue #38. | 2026-08-04 | Project owner; Role 3 |
| D-047 | Accepted | Add a trusted platform-neutral `system.vote-close` command. Role 1 schedules, authenticates, and deduplicates it at the authoritative 30-second deadline, loads the final accepted tally for the current session/cycle/revision, and passes candidate counts, accepted-vote count, and close time to Role 3 without selecting a winner. Role 3 alone decides majority, deterministic ties, zero-vote no-activation, cancellation, and winner legality; Role 1 then atomically persists and broadcasts the typed result. The first accepted vote per viewer is final for the MVP, late votes are rejected, and Twitch Extension, hosted-board, and Twitch-chat votes share the same ledger. | Keeps identity, vote acceptance, authoritative time, idempotency, persistence, and platform adapters with Role 1 while preserving Role 3's deterministic winner and activation authority. A command cannot prescribe or override the winner, and all participation surfaces resolve from one accepted tally. See issue #42. | 2026-08-05 | Project owner; Role 3 proposal |
| D-048 | Superseded by D-072 | Use Brawl Stars as the intended real-gameplay demonstration scenario if the current OBS capture spike proves the team can capture it reliably. If that proof fails, switch immediately to a team-owned PC action game. This is a calibrated demonstration choice, not a product or shared-contract restriction. | D-072 selects vanilla Minecraft as the calibrated demonstration target while preserving game-neutral contracts and retaining Brawl Stars only as evaluation evidence. | 2026-08-05 | Project owner |
| D-049 | Accepted | The minimum real extraction claim is universal action/quiet/transition intelligence plus at least one reliable calibrated fact for the selected demonstration game. Unsupported or uncertain specific facts remain `unknown`; the MVP does not claim universal health, kill, score, or phase recognition. | Provides judge-visible specificity while keeping the extraction claim achievable, evidence-based, and honest across games. Role 2 retains authority over algorithms, OCR, sampling, and confidence thresholds. | 2026-08-05 | Project owner |
| D-050 | Accepted | Twitch Extension Local/Hosted Test remains the primary viewer proof target. If it is not end to end by 6 August 2026 at 12:00 SGT, the real Twitch demonstration uses the hosted Viewer Quest Board and Twitch-chat voting fallbacks, with Extension readiness labelled truthfully. | Preserves the Twitch-first product direction while preventing Extension registration or integration delays from consuming the remaining real-workflow integration time. | 2026-08-05 | Project owner |
| D-051 | Accepted; finalist schedule superseded by D-077 | There is no automatic contract cutoff or feature-freeze date. Dated plan milestones are delivery targets only; the project owner alone explicitly calls the freeze. After that call, only the scope named by the project owner continues. The original proposal schedule named 9 August; the finalist schedule is superseded by D-077. | Keeps final scope authority with the project owner while preserving visibility of deadline risk and preventing lower-level plans from freezing work automatically. | 2026-08-05 | Project owner |
| D-052 | Accepted | Viewer participation rate—the share of active viewers who cast an accepted vote—is the principal product-impact metric. Supporting measures are quest completion, setup time, response latency, and fallback/reconnect success. | Directly measures ChatXPT's core value of converting passive viewers into participants and remains comparable across different audience sizes. | 2026-08-05 | Project owner |
| D-053 | Accepted | Final demo-narrative, slide-deck, and demo-video assembly are outside Role 1's current scope until the project owner explicitly declares the product ready. Evidence collection continues. Once activated, the baseline narrative is a rehearsed real workflow plus one controlled provider-unavailable case; prerecorded or simulated inputs cannot prove the live path. | Prevents premature presentation work from displacing product integration while retaining a truthful, resilient demonstration approach for the later submission phase. | 2026-08-05 | Project owner |
| D-054 | Accepted | Install `tesseract.js` pinned at `7.0.0` as the free local selective-OCR engine for Role 2's real-input evidence pass. Role 2 must keep it behind the injected `SelectiveOcrAdapter`, run it only on bounded named crops with local preprocessing, reuse workers, rate-limit bursts, measure p50/p95 latency, and report `unknown` when confirmation fails. Browser worker/CSP and language-data loading must be verified in the deployed target before OCR is cited as live evidence. | Removes the shared package-file blocker in issue #70 without adding a paid provider, API key, shared contract change, full-frame OCR, or live-evidence claim. It preserves credential-free fallback and keeps OCR accuracy/calibration ownership with Role 2 while Role 1 owns dependency and deployment safety. | 2026-08-09 | Project owner; Role 1; Role 2 proposal |
| D-055 | Superseded by D-072 | The judged MVP does not adopt or configure an external model provider. Role 2 uses the credential-free algorithmic candidate route for exactly-three candidate generation, and Role 3 keeps deterministic validation and replacement as the authority for unsafe, infeasible, malformed, missing, or rejected candidates. Groq `openai/gpt-oss-20b` is the first future controlled server-side trial candidate if Role 1 later supplies a free team-owned key, but it remains evaluation-only until real trial evidence clears the Role 2/3 rubric. | D-072 approves one bounded OpenAI path for the judged MVP while preserving the algorithmic route and Role 3 authority. | 2026-08-09 | Project owner; Role 2 recommendation; Role 3 rubric |
| D-056 | Accepted scope; execution order superseded by D-085 | Prioritise the next non-UI product scopes in this order: strengthen multi-game OBS/game-state capture and reliable gameplay understanding; strengthen adaptive intervention; add persistent streamer learning and profile memory; firm up the quest engine; and add automatic quest progress. Explainability/session history and the existing degraded-mode fallbacks are also required. Result-driven adaptation and community progression follow after the first five scopes. D-085 changes when this work begins relative to the owner-led snapshot and planning pass without removing these required scopes. | The current prototype already contains parts of these components, but the product must firm up its core intelligence and eventually shippable behaviour before it can be accepted as complete. | 2026-08-14 | Project owner |
| D-057 | Accepted | Treat game and platform universality as explicit contracts rather than immediate parallel integrations. Gameplay support must define normalised inputs, capabilities, confidence, freshness, `unknown`, and fallback behaviour for universal signals and game-calibrated adapters. Platform adapters must likewise document what ChatXPT consumes, what the external service must expose, what ChatXPT emits, and how capability loss degrades. YouTube, Discord, and other adapters remain unimplemented during the Twitch MVP. | Makes it possible to explain exactly why the current Twitch/OBS product works and what a future game or streaming platform must provide, without diluting the current build across incomplete integrations. | 2026-08-14 | Project owner |
| D-058 | Accepted | Persist streamer-owned identity, personality, preferences, restrictions, and learned cross-session behaviour so later sessions can adapt without repeated setup. Do not add persistent viewer-personality learning or cross-stream viewer profiles in the current scope; Twitch identity may continue to authorise participation, but viewer-personality memory is deferred until identity, consent, privacy, and value are designed deliberately. | Streamer retention is directly useful and controlled by the broadcaster, while persistent viewer modelling introduces substantially harder identity and privacy questions that are not yet settled. | 2026-08-14 | Project owner |
| D-059 | Accepted | Build and firm up the agreed feature scopes before the owner-led full integration testing, debugging, troubleshooting, real-world proof, and demo-rehearsal phase. Each implementation pass still keeps its normal automated, contract, and component verification so later integration work begins from a stable baseline. | Preserves the team's successful build-first, test-and-debug-together workflow while preventing unverified changes from accumulating between passes. | 2026-08-14 | Project owner |
| D-060 | Accepted | Automatic evidence may update partial quest progress only through the existing fresh, supported, game-matched, non-contradictory evidence gates. It may terminalise a quest as succeeded only when the active persisted quest state contains a predicate-bearing completion rule with an explicit target/comparison and the supplied validation context matches that stored rule exactly. An allowed signal kind or context-only rule cannot prove completion. Until that contract and integrated evidence exist, automatic success remains disabled and manual completion remains available. | Prevents an orchestrator/configuration mistake or a generic value of `1` from inventing quest success, rewards, or history while retaining useful conservative progress assistance. | 2026-08-17 | Project owner |
| D-061 | Accepted | Cross-session streamer learning may automatically apply only soft preferences such as tone, intensity, intervention timing, novelty, and quest variety. Learned values must be visibly identified, explainable, and resettable. Identity, selected game, safety restrictions, accessibility needs, and forbidden quest types remain hard streamer-owned settings and may never be changed silently; they require explicit streamer action. Persistent viewer-personality learning remains deferred under D-058. | Gives returning streamers meaningful adaptation without allowing inferred behaviour to erode consent, safety, accessibility, or control. | 2026-08-17 | Project owner |
| D-062 | Accepted scope; execution order superseded by D-085 | Stabilise universal activity, quiet, transition, capture-health, confidence, freshness, known/unknown/stale/unavailable, game-profile, and supported/unsupported-fact vocabulary, then integrate authenticated gameplay snapshots into the sole orchestrator. Calibrated HUD facts such as health, kills, score, and timers remain diagnostic-only until proven and may not be promoted into product claims by this pass. D-085 moves the owner-led snapshot and planning pass before this implementation work without expanding calibrated-game promises. | Preserves the universal-first, evidence-gated OBS/gameplay scope while following the owner's revised execution sequence. | 2026-08-17 | Project owner; Role 1 recommendation |
| D-063 | Superseded by D-071 | Use the existing D-015 Role 1 integration override for the approved isolated passes, including the minimum necessary integration and UI edits across Role 2-5 files. Preserve established component decisions, notify affected owners, request their review where practical, and do not treat the work as a permanent ownership transfer. | D-071 makes cross-role contribution normal for every pass and every contributor, so a special override is no longer required. | 2026-08-17 | Project owner |
| D-064 | Accepted | Keep the working root control-room and overlay prototype available until the canonical flow completes the seven-step demo outcome twice without manual repair. After parity, move the retained prototype behind a clearly labelled diagnostic route rather than deleting it immediately. | Protects the live fallback while preventing the legacy path from remaining the unlabelled product authority. | 2026-08-17 | Project owner |
| D-065 | Accepted | For the finals slice, implement a secure manual broadcaster-session start with real Twitch Extension JWT and channel mapping before requiring complete self-service OAuth, EventSub, and offline automation. Those automated setup capabilities remain required product follow-up after the real Extension flow works. | Reaches an authentic Twitch participation boundary without making optional automation a prerequisite for the first real Extension proof. | 2026-08-17 | Project owner |
| D-066 | Accepted | Treat the canonical Role 5 viewer presentation as the Twitch viewer UI authority and generate or align Twitch-uploadable static assets from that source. Do not maintain a separately designed static Extension viewer that can drift from `/viewer.html`. | Keeps interaction, accessibility, state disclosure, and visual changes consistent between local and Twitch-hosted viewer surfaces. | 2026-08-17 | Project owner |
| D-067 | Accepted | Keep `Sidequest` as ChatXPT's core product/domain term and use `Creator Mission Mode` only as a Garena-facing preset. Use `Gameplay Capture`, `Gameplay Activity`, `Capture Health`, `Signal Confidence`, `Detected Game Facts`, and `Game Profile` in OBS/intelligence UI; use `Active`, `Quiet`, `Transition`, and `Unknown` for activity; and use `Observed`, `Low confidence`, `Stale`, `Unavailable`, and `Permission denied` for evidence state. | Preserves the established product identity while giving universal gameplay evidence a clear, game-neutral vocabulary. | 2026-08-17 | Project owner |
| D-068 | Accepted | The Twitch Extension uses select-then-confirm voting because the first accepted vote is final. Quest cards remain rich, direct controls; the `1`/`2`/`3` Twitch-chat fallback is displayed only when Extension participation is unavailable rather than as the normal viewer experience. | Reduces accidental final votes and keeps the primary experience visibly Extension-native. | 2026-08-17 | Project owner |
| D-069 | Accepted | Choose the canonical or hybrid route as the primary finals demo only after it completes the seven-step workflow twice without manual repair. Otherwise use the rehearsed working prototype live, present the canonical architecture truthfully, and keep both primary and fallback routes ready. | Makes demo-route selection evidence-based while retaining a reliable recovery path. | 2026-08-17 | Project owner |
| D-070 | Accepted | After the bounded OBS pass, implement UI in isolated passes in this order: canonical Studio; Twitch Config and Live Config; Twitch Extension viewer; authoritative OBS overlay; hosted/chat fallback presentation; then visual polish and celebration states. | Stabilises authoritative vocabulary and data seams before dependent surfaces while keeping each visible integration pass bounded. | 2026-08-17 | Project owner |
| D-071 | Superseded in part by D-073 and D-076 | Replace exclusive role ownership with open cross-role contribution. Any contributor may inspect, edit, test, document, and implement in any role directory or collision-prone shared file without prior owner approval, reassignment, a cross-role issue, or a target-owner comparison. Role labels remain responsibility and review context, not permission gates. Runtime responsibilities, public-module/import boundaries, safety/privacy/security rules, checks, evidence requirements, and Role 1 integration coordination remain. Cross-role work must inspect current branches for overlap, notify relevant leads and Role 1, disclose affected files and judgement, run affected producer/consumer tests, and deconflict before merge. Role 1 actively helps resolve textual and semantic conflicts and preserves valid work from both sides. A responsibility lead may advise but may not block implementation, pushing, or opening a pull request merely because another role made the change. | D-073 removed the Role 1-only merge gate; D-076 removes mandatory review, branch-protection, and CODEOWNERS gates while keeping deconfliction and material-risk responsibilities. | 2026-08-18 | Project owner |
| D-072 | Accepted | ChatXPT may use the server-side OpenAI Responses API in the judged MVP with provider ID `openai` and exact model `gpt-5.6-terra`. Enablement is opt-in through server-only environment configuration and a team-owned `OPENAI_API_KEY`; the model has no OpenAI API free tier, so it may run only against existing team-owned prepaid or promotional credit. No contributor is required to buy quota, and adding a payment method or incurring new spend needs a separate owner decision. Missing credentials, credit, quota, or provider availability must keep or restore the credential-free algorithmic path. Send only bounded normalised gameplay, audience aggregates, streamer profile/restrictions, and recent-quest context: never raw frames, raw chat, usernames, Twitch IDs, viewer identity, or secrets. ChatXPT sets Responses `store: false`, retains no prompt, output, exception text, or vendor payload, and discloses that OpenAI may retain API customer content in abuse-monitoring logs for up to 30 days unless the team-owned API project is approved for Zero Data Retention. Use one attempt, an 8-second timeout, strict structured output for exactly three candidates, freshness/confidence/source-citation validation, and explicit refusal, cancellation, malformed-output, rate-limit, outage, and timeout handling. Every output still passes Role 3's deterministic safety, feasibility, evidence, diversity, lifecycle, and replacement authority. Vanilla Minecraft is the calibrated demonstration target; modded, hidden-HUD, or unrecognised variants retain universal signals and report calibrated facts as `unknown`. Brawl Stars remains evaluation evidence only, and all shared contracts stay game-neutral. D-071 governs contribution permissions: any contributor may implement this work in the module matching its runtime responsibility; Role 1 maintains and deconflicts runtime composition, environment configuration, credentials, and final integration rather than holding an exclusive file-edit gate. This supersedes D-048 and D-055 and the conflicting adoption/cost portions of D-014 and D-021. | Records the owner's direct approval in shared authority, removes the stale role/provider gates, makes the exact cost and privacy limits truthful, and preserves safe no-credential operation plus deterministic quest authority. | 2026-08-18 | Project owner |
| D-073 | Superseded by D-076 | Shared merge authority across the team and removed Role 1 as the exclusive merger, but still required independent review, branch protection/check gates, and pull-request-only integration. | D-076 supersedes those remaining mandatory review, branch-protection, CODEOWNERS, and direct-main-push prohibitions while retaining deconfliction, records, checks where relevant, and material-risk gates. | 2026-08-18 | Project owner |
| D-074 | Accepted; implementation scope narrowed by D-075 and D-082 | Use one authoritative session and sidequest state with three deliberately different projections for any Live Director feature retained after research. ChatXPT Studio and Twitch Live Config are private broadcaster/moderator decision surfaces for source-labelled Live Context, Chat Pointers, confidence/freshness/`unknown`, Director Cues, exactly-three proposal review, controls, health, and aggregate history. The OBS Browser Source is a public, read-only, low-distraction broadcast scoreboard for only universally important context, voting, winner, active sidequest, progress, shared hype, result, and reconnect state. The Twitch Extension is an individual viewer companion—not only a voting modal—for optional Catch-up, full quest details, exactly-three select-then-confirm voting, private acknowledgement/recovery, reactions, personal session points, expanded progress, result, and late-join continuity. Similar authoritative state may appear across surfaces, but full interfaces, private reasoning, raw chat, usernames, personal viewer fields, provider details, and commands must not be copied into OBS. D-075 retains the three-surface responsibility rule while narrowing implementation to the existing compressed OBS quest projection and making Catch-up a P1 experiment; D-082 adds one bounded `Up next` exception. | Gives duplicated state a distinct purpose: Studio supports private decisions, OBS supplies universal shared awareness and payoff, and the Extension supplies personal interaction, depth, accessibility, and persistence. It prevents both an over-cluttered broadcast and an underused voting-only Extension without pretending the unresearched feature value is already proven. | 2026-08-18 | Project owner |
| D-075 | Accepted; public `Up next` exception added by D-082 | Activate the research-bounded Live Director implementation as ten isolated passes split evenly between Role 1 and Role 3. P0 keeps the declared Session Goal/Current Objective, source-separated private Live Context, a narrow expiring Chat Pointer, one phase-aware Director Cue, the existing exactly-three validated sidequest conversion and streamer review, Extension Vote/Active/Result with private recovery, the existing compressed OBS quest projection, and private Live Config pop-out/OBS Dock delivery. A minimal Extension Catch-up Card and intervention-specific non-causal Session Brief are P1 experiments with explicit kill conditions. Defer private audio/earcons/hotkeys. Reject carry-forward AI coaching, an always-on-top desktop companion, gameplay explanation/strategy/narration, full chat summary/panel, generic AI cohost/producer, generic growth analytics, extra public OBS context, full catch-up timelines/highlights, viewer-paid game control, and persistent viewer profiling. Role 3 drives five deterministic cue/quest passes covering suitability, cue lifecycle/actions, exactly-three conversion, invalidation/emergency/history, and evaluation/handoff. Role 1 drives five passes covering canonical authority/privacy/projections, intent/pointer/context composition, streamer delivery, viewer/OBS delivery, and Session Brief/integration/value evidence. This split is responsibility and pickup clarity, not a permission, review, push, or merge gate under D-071/D-076; Role 1 deconflicts every wave. The retained scope is an evidence-gated product hypothesis: ChatXPT cannot create viewers, may not fabricate audience/game state or claim retention causality, and must preserve the credential-free algorithmic path, Role 3 deterministic fallback/validation authority, Twitch Extension primary participation, hosted-board fallback, and Twitch-chat fallback. | Converts the completed secondary research into the smallest falsifiable product loop that may add value beyond Twitch's existing chat, poll, analytics, Extension, and OBS primitives. It gives each contributor a coherent half while preserving one runtime authority and explicit merge seams, and keeps weakly evidenced or occupied product categories outside implementation. D-082 permits only the bounded public `Up next` statement; strategy, coaching, play-by-play narration, and unproven `Now` claims remain rejected. | 2026-08-19 | Project owner; Role 1 records the owner-approved split |
| D-076 | Accepted | Remove mandatory review, branch-protection, and CODEOWNERS merge gates. Pull requests remain the normal coordination and evidence record, but they are preferred workflow rather than an approval gate. Any contributor with repository write or merge permission may merge a pull request or land a deliberate direct integration commit, including their own work, after inspecting the diff, syncing or deconflicting current `main` and overlapping branches, running the relevant checks or documenting why they were not run, updating required TODO/change/evidence records, and confirming no unresolved material safety, security, privacy, data-loss, external-cost, or broken golden-workflow risk remains. Responsibility-lead review, Role 1 review, and independent review are advisory and may be requested when useful, but a missing response is not a blocker. Branch protection should be disabled for this repository; if GitHub still enforces it, that is an external settings mismatch with this decision rather than project policy. | The project owner explicitly trusts the role contributors to land deconflicted work without review or branch-protection bottlenecks, while preserving accountability through repository records, checks where relevant, and mandatory escalation only for material risk. | 2026-08-19 | Project owner |
| D-077 | Accepted | Keep every contributor and agent work-conserving: nobody waits solely for another role, owner, review, reply, or responsibility handoff. If a repository dependency is missing, the contributor inspects overlap, notifies the relevant leads and Role 1, and either implements the smallest coherent cross-role seam in its proper module under D-071 or immediately selects another ready P0 task. Notification and advisory review never pause safe implementation. Missing external credentials/platform access, unresolved material safety/security/privacy/data-loss/external-cost risk, an authority-level semantic conflict, or a failing required integration boundary remain genuine blockers; they must be reported honestly without fabricating evidence, while other safe work continues. Role 3 is immediately authorised to continue through the outstanding Role 1 composition/viewer/OBS seam and predicate-bearing automatic-completion seam instead of monitoring their owners. The Top 10 finalist brief sets the current immutable Drive deadline at 21 August 2026, 3 PM SGT, superseding the earlier 9 August proposal schedule without declaring a feature freeze. | Removes stale owner-wait instructions while preserving architecture, evidence truth, and material-risk gates; it also aligns repository authority with the current finalist schedule. | 2026-08-20 | Project owner |
| D-078 | Accepted | Treat every customer-facing ChatXPT surface as a final product for its intended streamer or viewer. Product UI must use customer-facing language and must not address testers or developers, show fixture/test disclaimers, or discuss workflow/evidence status. When a product surface is rendered from fixtures for development or review, the surrounding diagnostic harness and repository evidence records—not the product UI—own that disclosure. | ChatXPT's product surfaces are for streamers and viewers, not for communicating with testers. | 2026-08-20 | Project owner |
| D-079 | Accepted | Add a bounded streamer-only `Live Analytics` surface for current audience health. Live Director may supply aggregate chat energy, activity, mood, participation, repeated-topic, keyword-count, freshness, and current-session timeline signals. This surface is not generic growth analytics or a full chat panel: it does not retain or display raw chat messages, usernames, viewer identity, or persistent viewer profiles, and it must not claim that a signal caused retention or growth. | Gives streamers an obvious view of live audience health while preserving the accepted raw-chat minimisation, viewer-privacy, and non-causal analytics boundaries. | 2026-08-20 | Project owner |
| D-080 | Accepted | Keep shared contracts and core orchestration game-neutral, but make every displayed quest objective—including credential-free algorithmic and deterministic-library fallbacks—game-aware. Candidate language must use the selected game profile and only fresh supported gameplay facts; it may describe a mechanic known for that game without pretending an unobserved state is current. A state-specific suggestion contradicted by evidence is invalid. When ChatXPT cannot produce enough safe, feasible, game-compatible options, an idle Home preview may show fewer than three or none; an official proposal/voting batch still requires exactly three validated candidates and otherwise returns typed exhaustion rather than generic filler. | Prevents technically reusable architecture from becoming vague or contextually wrong streamer copy, such as suggesting entry into a closing danger zone, while preserving the exactly-three voting contract and honest unknown states. | 2026-08-20 | Project owner |
| D-081 | Deferred; previously accepted | Defer streamer audio and speech capture from the current product surface and completion criteria. Do not expose microphone or transcript controls, include audio in the current snapshot review, or make audio-completion claims. Revisit speech only after the core live gameplay, chat, and quest loop works end to end. | Keeps the current pass focused on completing and proving the core product loop before adding another live input. | 2026-08-20 | Project owner |
| D-082 | Accepted | For the current MVP, the public OBS overlay may show one concise `Up next` activity line, but not a `Now` claim. Source it only from the streamer's typed Current Objective or the authoritative active quest, require compatibility with the selected game and fresh gameplay evidence, and hide the line when evidence is insufficient. Audio is not a current input under D-081; transcripts and private reasoning never enter OBS. This supersedes D-074 and D-075 only for this bounded `Up next` exception. | Gives viewers useful game-aware orientation without claiming that the current visual engine can prove a specific activity or exposing private Live Director context. | 2026-08-20 | Project owner |
| D-083 | Accepted | A feature reaches technical acceptance only when its visible UI, authoritative command, persistence, realtime update, downstream viewer or OBS reflection where applicable, recovery state, and relevant real-input proof work together. Automated or component evidence alone does not mark it complete. The project owner personally tests the integrated product and holds final authority to mark the feature complete; contributors still run and report the normal technical checks without claiming owner acceptance. | Prevents isolated components, fixtures, or passing tests from being represented as a finished product while keeping technical verification separate from the owner's final hands-on acceptance. | 2026-08-20 | Project owner |
| D-084 | Accepted | Keep Test Lab accessible to every streamer from the normal Studio navigation for the current product. Do not require an owner or developer mode. Its navigation access may be removed later through a separate owner-directed UI change. The authorised-input, fixture-labelling, and live-evidence boundaries in D-027 remain in force. | Gives streamers direct access to the Test Lab now without treating its diagnostic or fixture output as proof of live operation. | 2026-08-20 | Project owner |
| D-085 | Accepted; sequence clarified | Create and publish the complete implementation plan and unavailable-capability inventory now. Before product source implementation begins, the owner cues proposed final-interface snapshots, reviews and changes them, and the team refreshes the plan wherever those accepted designs change scope or acceptance criteria. Implementation then proceeds in end-to-end vertical slices. Snapshots are design targets rather than runtime evidence. This supersedes the runtime-first sequencing in D-056 and D-062 without removing their required gameplay, intelligence, persistence, quest-engine, progress, history, or fallback work. | Gives the other development computer an authoritative plan immediately while preserving the owner's snapshot review as the implementation gate and the end-to-end evidence bar for every feature. | 2026-08-20 | Project owner |
| D-086 | Accepted | Strengthen the Minecraft demonstration path beyond a fixed vanilla-HUD-layout check. The primary demo target is Vanilla Minecraft Java Survival with default HUD, but ChatXPT should treat modded or altered HUDs as best-effort evidence: Minecraft-like hearts, hunger, hotbar, inventory, sleep/menu, damage, and activity cues may be detected through pixel-aware search and confidence scoring when visual patterns remain recognisable, while heavily changed texture packs, hidden HUDs, or ambiguous evidence must degrade to `unknown`. The next game-state schema pass should separate game facts, streamer intent, and active ChatXPT quest state; include Minecraft-specific activity and context such as exploring, mining, building, combat, crafting, inventory/menu, sleeping, recovering, biome or environment when supported, visible hostile presence, recent damage, likely damage cause, health/hunger/armor/hotbar observations, supported facts, unknown facts, confidence, freshness, and evidence source; and preserve Role 3 as the deterministic authority before any candidate reaches viewers. Claim level may be stronger than layout-only: ChatXPT may say it is a Minecraft-aware engine that detects supported HUD/game-state signals and gracefully degrades for modded or unsupported evidence, but it may not claim exact facts such as mob type, biome, damage cause, health, hunger, or objective completion unless the detector or approved model supplies confidence-scored evidence and the evidence is recorded. AI communication must use typed known/unknown facts, source ids, confidence, streamer goal, active quest, restrictions, and freshness instead of vague prose. Stronger AI models may improve language and supported inference only after the same privacy/cost/evidence gates in D-072; weak models must still receive the same schema and rely on deterministic validation/fallback. Raw frames or screenshots remain excluded from provider input unless the owner separately approves a vision-model privacy/cost expansion. | The current Minecraft profile is too shallow to prove clear value-add. The accepted direction preserves the honest `unknown` policy while making the engine meaningfully Minecraft-aware through better visual search, richer structured game state, streamer intent, and safer AI contracts. | 2026-08-20 | Project owner |
| D-087 | Accepted; supersedes the post-stream deferral in D4-69 and the integrated completion plan | Treat persistent streamer profiles/presets and post-stream history/analytics as two separate initiatives, both required. Neither initiative may change or become a dependency of the existing golden demo flow; the only normal demo-flow effect is restoring the verified streamer's saved presets. Preserve the current local preset/state-persistence method as the degraded fallback when Supabase, Vercel, or other hosted infrastructure is unavailable. | Adds both required persistence outcomes without destabilising the working demonstration and preserves a usable local fallback during hosted-service failure. | 2026-08-22 | Project owner |
| D-088 | Accepted | Show a session in Stream History only when it actually reached `live`. Twitch connection, setup attempts, Test Lab activity, and abandoned preparing sessions are not past streams and must not appear in Stream History. | Keeps streamer history aligned with broadcasts that actually started and prevents setup or diagnostic activity from being misrepresented as stream history. | 2026-08-22 | Project owner |
| D-089 | Accepted | Implement persistent streamer profiles/presets before the separate history/analytics initiative. When Supabase is unavailable, preserve the current local preset store and allow local use and editing. A later cloud sync may apply a local change only when its recorded base cloud revision is still current; otherwise the streamer chooses which version to keep, and neither version is silently overwritten. Twitch may refresh verified display and current-stream game metadata but may not silently rewrite saved presets, restrictions, accessibility settings, or preferred game settings. | Makes history depend on stable ownership, preserves a usable degraded mode, and prevents recovery or OAuth refresh from destroying streamer-owned configuration. | 2026-08-22 | Project owner |
| D-090 | Accepted | Publish a qualifying stream in Stream History only after it reaches a terminal end; an active stream stays in Live Analytics. Manual end, confirmed Twitch offline, and reconnect-grace expiry must finalise the same single history entry. Retain private vote/deduplication records for 30 days, detailed analytics windows for 90 days, final stream summaries/quests/results/aggregate analytics for 365 days, and account/identity/presets until explicit deletion. Show valid legacy streams with incomplete evidence as `Limited analytics` rather than hiding them or inventing data. Retained analytics may include duration, game, capture coverage, known/unknown gameplay aggregates, privacy-safe audience aggregates, quest outcomes, votes, and rewards, but never recordings, raw frames, raw chat, usernames, persistent viewer profiles, or causal audience-growth claims. | Gives streamers useful, honest history with bounded retention and prevents duplicated stream records or privacy-invasive analytics. | 2026-08-22 | Project owner |
| D-091 | Accepted | Preserve the existing fake local-login presentation as an explicit in-app route to one pre-established local demo account with usable saved local presets when Supabase is unavailable or the streamer deliberately selects local fallback. This account is device/local-runtime recovery state only: it is not ChatXPT authentication, does not grant hosted or Twitch authority, cannot read cloud profiles/history, and must be visibly distinguished from the verified Twitch-backed account path. Twitch OAuth remains the real MVP streamer identity. | Ensures the owner can recover a failed demo without rebuilding presets while keeping real account authority and cloud data isolation truthful. | 2026-08-22 | Project owner |
| D-092 | Accepted | Keep the future ownership schema capable of linking multiple verified provider identities to one internal ChatXPT account, while the Twitch MVP UI operates with one active verified Twitch broadcaster at a time. Disconnecting Twitch preserves the account, presets, and history; permanent deletion is a separate explicit action. Prepare schema-safe deletion boundaries now, but defer the visible deletion UI until the real ChatXPT account system is introduced. | Allows later account linking without data migration or accidental loss while keeping current Twitch behavior simple and deletion deliberate. | 2026-08-22 | Project owner |
| D-093 | Accepted; supersedes D-075 and the Role 4 plan exclusion only for this bounded companion | Add a private Desktop Live Director companion that reuses the existing broadcaster-linked, read-only Live Director projection in a small resizable window. It defaults to always on top, supports click-through and hide/show shortcuts, remembers placement and display preferences, stores its permanent private grant through operating-system encryption, resolves the broadcaster's active session across restarts, and remains separate from OBS/public broadcast output. The macOS package is the current delivery target; the Electron runtime remains portable for later Windows packaging. Keep the browser pop-out and OBS Custom Dock as fallbacks. Do not inject into the game process, duplicate quest authority, expose private grants in logs, or claim that Display Capture will exclude the private window. | The owner streams Minecraft in a normal window and needs the private Live Director to remain visible after Minecraft receives focus. Reusing the existing private projection gives that outcome without changing the golden workflow, public overlay, Twitch Extension, quest authority, or server secrets. | 2026-08-23 | Project owner |
| D-094 | Accepted; supersedes D-093 only for the companion's read-only restriction | Keep the permanent encrypted broadcaster link, but allow the private Desktop Live Director to select one of the current exactly-three recommendations and emit the same narrowly scoped `streamer.quest` approval used by Studio. The server authenticates the permanent broadcaster grant, re-resolves the current session and cycle, validates the selected candidate and current allowed action, and lets the existing Role 1/Role 3 authority open viewer voting and broadcast the new revision to every surface. The companion does not calculate a winner, cast or fabricate a viewer identity, bypass the exactly-three vote, or gain general Studio command authority. Also show live duration with Session phase, derive `Peaceful`/`Steady`/`Hype` from current chat evidence with a truthful quiet-live fallback, and place the current gameplay-feed state such as `Stable` in the header. | Gives the streamer one low-distraction approval control while gaming and fixes ambiguous status presentation without moving lifecycle, voting, or persistence authority into the desktop client. | 2026-08-23 | Project owner |
| D-095 | Accepted; supersedes D-094's candidate-selection and vote-opening behaviour | Make quest routing mode-specific in Studio and the private Desktop Live Director. In `Automatic`, the streamer may generate exactly three validated options and push the complete batch without selecting a candidate; viewers vote during the authoritative voting window and the winner activates automatically. In `Manual`, the streamer selects exactly one current validated option and starts it directly; no viewer vote, voting countdown, or vote tally is created. Both surfaces emit canonical commands, Role 1 authenticates, persists, and broadcasts them, and Role 3 alone decides the transition. The Desktop companion gains only quest generation and the mode-appropriate proposed-cycle action, not winner, vote, general lifecycle, or persistence authority. | Removes a meaningless streamer pick from Automatic mode and unnecessary viewer voting from Manual mode while preserving the golden viewer-led Automatic flow. | 2026-08-23 | Project owner |
| D-096 | Accepted; supersedes D-095 only for active-quest Desktop controls | While the private Desktop Live Director displays an authoritative active quest, allow the broadcaster to request `cancel` and `succeed` through the same permanent encrypted grant. `Cancel quest` requires an explicit confirmation; `Mark complete` remains immediate like Studio's success action. The server must re-resolve the current broadcaster, session, cycle, revision, active candidate, and allowed action; accept only `cancel` or `succeed` for this active-state expansion; persist and broadcast through Role 1; and let Role 3 decide the terminal transition and reward. The companion still cannot fail, skip, pause, choose a winner, cast a vote, set progress, or exercise general Studio/session/persistence authority. | Gives the streamer the two essential active-quest outcomes without leaving the always-on-top gameplay surface, while preserving least privilege, destructive-action confirmation, idempotency, and the existing authoritative lifecycle. | 2026-08-23 | Project owner |

When the team settles a decision, change its status to `Accepted`, add the date and participants, and document any replacement explicitly.
